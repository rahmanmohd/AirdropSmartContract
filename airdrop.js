// airdrop.js
import AirdropPage from "../components/AirdropPage";

const Airdrop = () => {
  const contractAddress = "";
  const tokenAddress = "";
  const network = "";

  return <AirdropPage contractAddress={contractAddress} tokenAddress={tokenAddress} network={network} />;
};

export default Airdrop;
